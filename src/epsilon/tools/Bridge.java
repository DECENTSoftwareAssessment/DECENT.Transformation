package epsilon.tools;

public class Bridge {

	public void run(Object o) {
		System.out.println(o.getClass());
		//first attempt:
		  //convert input to Weka?
		  //run Weka on input
		  //return output in some reasonable format (object?)
		  //integrate output into model
		//alternatively:
		  //integrate output and modify model in-place?
	}
}
